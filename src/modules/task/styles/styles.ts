import { StyleSheet } from 'react-native'

const gap = 28

export const styles = StyleSheet.create({
  container: {
    borderColor: 'darkgray',
    borderRadius: 4,
    borderWidth: 1,
    padding: 16
  },
  taskName: {
    fontSize: 22,
    fontWeight: 'bold'
  }
})
