import { SafeAreaView, Text } from 'react-native'

export const FilterModal = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        padding: 12,
        backgroundColor: 'white',
        borderRadius: 24
      }}
    >
      <Text>Ini filter modal</Text>
    </SafeAreaView>
  )
}
