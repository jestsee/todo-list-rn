import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: { width: '100%' },
  subtitle: {
    marginBottom: 16,
    marginTop: 2,
    textAlign: 'center'
  },
  title: {
    fontSize: 36,
    fontWeight: 'bold',
    textAlign: 'center'
  }
})
