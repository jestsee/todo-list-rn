import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    borderRadius: 4,
    paddingHorizontal: 16,
    paddingVertical: 4
  },
  fill: {
    backgroundColor: 'red'
  },
  outline: {
    borderColor: 'red',
    borderWidth: 1
  }
})
